Got you, Gomathy — **only interface endpoints**, no gateway endpoints at all.  
So here are the **correct Terraform data sources** for the interface‑based services you asked for:

- **SSM**
- **SSM Messages**
- **EC2 Messages**
- **Secrets Manager**
- **Snowflake PrivateLink**
- **(Optional) S3 *interface* endpoint** — only available in some regions/accounts, not common, but I’ll include it since you said “all interface”.

Everything below is **interface‑only**.

---

# 🌟 **1. SSM (Interface Endpoint)**

```hcl
data "aws_vpc_endpoint" "ssm" {
  vpc_id            = var.vpc_id
  service_name      = "com.amazonaws.${var.region}.ssm"
  vpc_endpoint_type = "Interface"
}
```

---

# 🌟 **2. SSM Messages (Interface Endpoint)**  
Required for SSM Agent to work.

```hcl
data "aws_vpc_endpoint" "ssm_messages" {
  vpc_id            = var.vpc_id
  service_name      = "com.amazonaws.${var.region}.ssmmessages"
  vpc_endpoint_type = "Interface"
}
```

---

# 🌟 **3. EC2 Messages (Interface Endpoint)**  
Also required for SSM Agent.

```hcl
data "aws_vpc_endpoint" "ec2_messages" {
  vpc_id            = var.vpc_id
  service_name      = "com.amazonaws.${var.region}.ec2messages"
  vpc_endpoint_type = "Interface"
}
```

---

# 🌟 **4. Secrets Manager (Interface Endpoint)**

```hcl
data "aws_vpc_endpoint" "secretsmanager" {
  vpc_id            = var.vpc_id
  service_name      = "com.amazonaws.${var.region}.secretsmanager"
  vpc_endpoint_type = "Interface"
}
```

---

# 🌟 **5. Snowflake PrivateLink (Interface Endpoint)**  
Snowflake gives you a **service ID** like `vpce-svc-0abc123def456`.

### Step 1 — Look up the service  
```hcl
data "aws_vpc_endpoint_service" "snowflake" {
  service_name = "com.amazonaws.${var.region}.vpce-svc-${var.snowflake_service_id}"
}
```

### Step 2 — Look up the actual endpoint  
```hcl
data "aws_vpc_endpoint" "snowflake" {
  vpc_id            = var.vpc_id
  service_name      = data.aws_vpc_endpoint_service.snowflake.service_name
  vpc_endpoint_type = "Interface"
}
```

---

# 🌟 **6. S3 *Interface* Endpoint (only if enabled in your account)**  
Most AWS accounts use **gateway** for S3, but if your org has enabled **S3 PrivateLink**, this is the interface version:

```hcl
data "aws_vpc_endpoint" "s3_interface" {
  vpc_id            = var.vpc_id
  service_name      = "com.amazonaws.${var.region}.s3"
  vpc_endpoint_type = "Interface"
}
```

If your account does **not** support S3 interface endpoints, this data source will fail — which is expected.

---

# 🎯 **Final Summary (Interface‑only Endpoints)**

| Service | Endpoint Type | Terraform Data Source |
|--------|----------------|------------------------|
| SSM | Interface | `com.amazonaws.<region>.ssm` |
| SSM Messages | Interface | `com.amazonaws.<region>.ssmmessages` |
| EC2 Messages | Interface | `com.amazonaws.<region>.ec2messages` |
| Secrets Manager | Interface | `com.amazonaws.<region>.secretsmanager` |
| Snowflake | Interface | `com.amazonaws.<region>.vpce-svc-xxxx` |
| S3 (optional) | Interface | Only if S3 PrivateLink enabled |

---

If you want, I can also generate:

- A **module** that automatically discovers all required interface endpoints  
- A **validation script** that fails the plan if any endpoint is missing  
- A **diagram** showing how private subnets use these endpoints  

Just tell me what you want next.
